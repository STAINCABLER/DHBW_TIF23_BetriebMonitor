# shellcheck shell=bash
# shellcheck disable=SC2034
# AlteBank Web3.0 Konfiguration
# Kopiere diese Datei zu ".env" und fülle die Werte aus.

# Upstash Redis REST Endpoint (Format: https://<host>.upstash.io)
UPSTASH_REDIS_REST_URL=

# Upstash Redis REST Token
UPSTASH_REDIS_REST_TOKEN=

# Optional: Klassischer Redis Connection-String (wird zu REST-Daten abgeleitet)
REDIS_URL=

# Flask Secret Key (für Session-Signaturen)
APP_SECRET_KEY=change-me

# Lebensdauer der API-Sessions in Sekunden (optional)
SESSION_TTL_SECONDS=3600

# Eindeutige Kennung dieser Bankinstanz (z. B. "bank-stuttgart")
INSTANCE_ID=

# Öffentliche URL bzw. Hostname der Instanz (für Föderationspartner)
INSTANCE_HOST=

# Persistentes Verzeichnis für Instanzschlüssel und Zertifikate (wird automatisch befüllt)
INSTANCE_TLS_DIR=./data/tls

# Optional: Zusätzliches Secret für die Verschlüsselung von Nutzer-Schlüsseln
USER_KEY_ENC_SECRET=

# Optional: Öffentlicher Schlüssel der Bank (für Auszahlungen als Gegenpartei)
BANK_PUBLIC_KEY=BANK_SYSTEM
